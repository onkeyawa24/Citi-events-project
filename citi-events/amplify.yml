version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd citi-events
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: citi-events/build
    files:
      - '**/*'
  cache:
    paths:
      - citi-events/node_modules/**/*
